interface Language {
    name: string;
    shortName: string;
}

interface CreateLanguageForm {
    name: string;
    shortName: string;
}

interface EditLanguageForm {
    name: string;
    shortName: string;
}
